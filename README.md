# quest.client.admin
Web admin for quest

# Setup Angular
```sudo apt-get install nodejs npm```

```sudo npm install -g npm@latest```

```sudo npm install -g @angular/cli```

From working folder:

```npm install```
